import { Metadata } from 'next';
import React from 'react';
export const metadata: Metadata = {
  title: 'Not Found',
};

const NotFound = () => {
  return <h1>404NonFound</h1>;
};

export default NotFound;
